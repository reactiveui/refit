using System;
using System.Net.Http;
{{#usingList}}
using {{usingItem}};
{{/usingList}}

{{#classList}}
namespace {{namespace}}
{
    public class AutoGeneratedImpl__{{interfaceName}} : {{interfaceName}}
    {
        readonly HttpClient client;
        readonly Dictionary<string, Func<HttpClient, object[], object>> methodImpls;

        public AutoGeneratedImpl__{{interfaceName}}(HttpClient client, IRequestBuilder requestBuilder)
        {
            methodImpls = requestBuilder.InterfaceHttpMethods.ToDictionary(k => k, v => requestBuilder.BuildRestResultFuncForMethod(v));
            this.client = client;
        }

{{#methodList}}
        public {{returnType}} {{name}}({{argumentListWithTypes}})
        {
            var arguments = new object[] { {{argumentList}} };
            return ({{returnType}}) methodImpls["{{name}}"](client, arguments);
        }
{{/methodList}}
    }
}
{{/classList}}
